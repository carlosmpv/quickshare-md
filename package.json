{
  "name": "quickshare",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "bun build ./index.html --minify --target=browser --outdir dist",
    "dev": "bun build ./index.html --minify --target=browser --outdir dist --watch",
    "serve": "bunx serve dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dompurify": "^3.3.1",
    "marked": "^17.0.3",
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "serve": "^14.2.5"
  },
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}
